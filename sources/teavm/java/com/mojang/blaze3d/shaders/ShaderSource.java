package com.mojang.blaze3d.shaders;

/**
 * EaglerCraft stub for MC 26.1.2 ShaderSource.
 *
 * Represents a source of shader programs (precompiled or compiled at runtime).
 * Browser: WebGL2 shaders are managed by PlatformOpenGL, this stub just
 * satisfies TeaVM compilation.
 */
public interface ShaderSource {
    // Marker interface — actual methods vary by MC version.
    // MC code mostly passes instances around without calling methods on them
    // at the call sites TeaVM currently reaches.
}
