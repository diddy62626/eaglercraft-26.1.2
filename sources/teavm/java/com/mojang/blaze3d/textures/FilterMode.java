package com.mojang.blaze3d.textures;

/**
 * EaglerCraft stub for MC 26.1.2 FilterMode.
 *
 * Controls texture filtering (nearest/linear). Browser: WebGL2 supports
 * these but we just need the enum for compile.
 */
public enum FilterMode {
    NEAREST,
    LINEAR;
}
